min_version = "2026.6.14"

[tools]
go = "1.26"
python = "3.12"
uv = "0.9.2"

[tasks.test]
description = "Run the GitHub workflow black-box E2E suite"
run = "uv run --python 3.12 --with pytest==8.3.5 --no-project pytest -q tests/e2e"
