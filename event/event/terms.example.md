# Participant terms

Replace this file during organizer bootstrap. Its exact UTF-8/LF bytes are
hashed into the signed event configuration.

The event is public and mutually distrustful: request files, Git history, PRs,
and normalized public registry records may be visible to every participant.
Never commit private keys, plaintext confidential submissions, or secrets.
